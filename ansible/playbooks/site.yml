- name: Provision Resume Vault server
  hosts: "{{ target_group | default('production') }}"
  become: true
  roles:
    - role: common
      tags: common

    - role: nodejs
      tags: nodejs

    - role: python
      tags: python

    - role: postgresql
      tags: postgresql

    - role: docker
      tags: docker

    - role: redis
      tags: redis

    - role: backend_env
      tags: backend_env

    - role: nginx
      tags: nginx

    - role: uvicorn
      tags: uvicorn
      
    - role: monit
      tags: monit