# production.yml
---
cors_origins: "https://{{ domain_name }},https://www.{{ domain_name }}"
certbot_domains:
  - "{{ domain_name }}"
  - "www.{{ domain_name }}"
certbot_cert_name: "{{ domain_name }}"
nginx_server_names: "{{ domain_name }} www.{{ domain_name }}"
